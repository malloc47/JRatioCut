/* Likely a better way to handle these variables, but 
 * I'm approximating a C-style rewrite of a scheme 
 * program with Java, so some concessions are expected. */
public class Params {
  public static boolean debug = false;
  public static boolean verbose = false;
  public static double alpha = 0.5;
}
